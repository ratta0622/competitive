#include <bits/stdc++.h>
using namespace std;

int main(){
  int N, M, R;
  cin >> N >> M >> R;
  int r[R], A[M], B[M], C[M];
  for(int i=0; i<R; ++i){
    cin >> r[i];
  }
  for(int i=0; i<M; ++i){
    cin >> A[i] >> B[i] >> C[i];
  }
  
  

  return 0;
}
